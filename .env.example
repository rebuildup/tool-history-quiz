# Environment variable schema for tool-history-quiz.
#
# Copy to `.env` (development) or `.env.production` and fill in real values.
# `.env*` files are Git-ignored. Do NOT commit real values.
#
# This tool currently has no required environment variables. Add entries here
# when introducing new runtime configuration so the schema stays the source of
# truth between local development and GitHub Secrets / CI.

# PUBLIC_VALUE=safe-example